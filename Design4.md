# Design4.md — FWAlizer 设计记录（当前）

> **文档定位：** 本文档是 FWAlizer 的**当前设计记录**（设计大方向、架构构想、决策记录，非强制，供参考），承接已存档的 [Design1-3](HistoryDocs/)（见 `HistoryDocs/`，仅核查）。
> 编码约束遵循 [AGENTS.md](./AGENTS.md)（**唯一强要求**）；详细构建方案见 [Build5.md](./Build5.md)；问题追踪见 [Issue4.md](./Issue4.md)。
> 设计状态：截至 2026-09-21，既有功能已完成；仓库身份已收束为 `cloudhost-firewall-autoupdater`，Desktop 开发范围已正式移除。

---

## 一、当前功能全景（截至 2026-09-21）

### 1.1 运行模式

| 模式 | 定位 | 说明 |
|------|------|------|
| **WebUI 模式**（默认） | 首选 | 浏览器可视化管理（`127.0.0.1:60200`），SQLite 持久化，配置热重载，推荐所有用户使用 |
| `.env` 模式 | 备用/进阶/极简 | 纯 headless，环境变量驱动，适合服务器/容器无界面场景 |

项目交付范围仅包含 WebUI 单二进制、`.env` headless 与 Docker，不包含桌面托盘、开机自启或原生桌面打包计划。

### 1.2 核心能力

- **多云同步**：腾讯云 Lighthouse/CVM、阿里云 SWAS/ECS 四款云产品统一管控；仅增量添加 + 精确删除带 `[TAG]` 标记的规则，绝不误删手动规则；
- **DNS 解析**：A + AAAA 双栈解析（域名规则可独立开关 IPv6）、10s 超时、渐进式熔断（半开探测自动恢复）；
- **同步引擎**：`time.Ticker` 调度（无外部 cron）、同步全局开关（暂停/开启，DB 持久化）、跨云并行 + 同云串行限速、乐观锁重试（最多 3 次）；
- **模拟测试**：按当前云资源目标与域名规则计算变更预览（不实际写入），暂停状态下可用；连接测试保留在目标添加/编辑弹窗（15s 超时）；
- **资源扫描**：设置页按厂商+地域一键扫描云资源（实例/安全组），结果持久化，添加目标时自动补全资源 ID 并联动填入地域；
- **告警**：邮件（SMTP）+ Webhook（钉钉/飞书/Slack）双通道，同步失败/DNS 失败自动推送；
- **Docker**：Alpine 镜像、非 root、HEALTHCHECK、单二进制分发。

### 1.3 WebUI 功能清单（当前状态）

| 页面 | 功能 | 关键设计决策 |
|------|------|-------------|
| 仪表盘 | 2×2 大卡片（同步引擎/上次同步/统计概览/操作中心）+ 首次使用引导 | 状态唯一展示（大字+状态色）；无模拟测试入口（菜单栏唯一） |
| 云资源管理 | 目标 CRUD + 弹窗表单级测试连接 + 资源 ID 按平台提示（轻量云=实例 ID，CVM/ECS=安全组 ID）+ 扫描结果自动补全 + 资源-地域联动 | Keys 缺失时提示（不阻止保存） |
| 域名规则 | 规则 CRUD + 适用目标（中文云产品名）+ IPv6 开关 | 每规则独立 IPv6 解析开关；弹窗适配 16px 字号（label 单行） |
| 全局设置 | 凭据卡片化（腾讯云/阿里云分卡）+ 一键扫描云资源（按厂商聚合展示）+ TAG/间隔/DNS/日志级别 + 地域自动补全 + 配置导入导出（含凭据提示）+ 清空所有数据 | 凭据与资源声明分离；危险操作为红色警告 + 二次确认 |
| 同步日志 | 历史记录（计数/错误弹窗/清空/刷新）+ 实时运行日志（常驻展开） | 计数链路打通；日志与终端格式一致 + 历史回放 |
| 模拟测试 | 变更预览（三级分组明细，不实际写入；说明文字含使用建议） | 单板块页（路由 /dry-run）；连接测试保留在目标弹窗 |
| 告警配置 | 邮件 + Webhook 开关与表单 | 修改即时生效 |

### 1.4 全局体验

- **明暗主题**：Naive UI `darkTheme` 动态切换，`settings` 表 `theme` 键 DB 持久化（多端一致）；侧边栏 ☀️/🌙 图标 + tooltip；
- **首次引导**：无凭据时仪表盘顶部引导条引导「先填 Keys → 再加目标/规则」；
- **实时日志**：环形缓冲 1000 条历史回放 + `slog.TextHandler` 格式（与 `docker compose logs` 逐字符一致）。

### 1.5 2026-08-03 新增功能（全部已完成）

| 功能 | 说明 | 状态 |
|------|------|------|
| 资源 ID 按平台提示 | 添加目标/连接测试时 placeholder 按平台区分：轻量云=实例 ID（lhins- 开头/UUID 格式），CVM/ECS=安全组 ID（sg- 开头） | ✅ 已完成 |
| 地域自动补全 | 后端提供四平台地域/可用区预填数据（数据源自 `PlatformZoneGuide/`），前端下拉可过滤选择，也可自由输入任意地域（兼容云平台新区域） | ✅ 已完成 |
| 凭据卡片化 + 资源扫描 | 设置页腾讯云/阿里云凭据分卡片展示；卡片内选择云产品+地域后一键扫描资源（实例/安全组列表，只读 API），结果持久化并按厂商聚合展示（含产品类型列） | ✅ 已完成 |
| 扫描结果自动补全 | 添加目标/连接测试时资源 ID 下拉选择扫描结果，选择后自动联动填入所在地域 | ✅ 已完成 |
| 清空所有数据 | 设置页一键清空全部业务数据（目标/规则/凭据/日志/扫描结果）重新初始化，红色警告 + 卡片式二次确认 | ✅ 已完成 |
| 导入导出配置提示 | 导出前提示配置不含凭据；导入前警告将替换现有数据并清空凭据（需重新填写） | ✅ 已完成 |
| 全局 UI 统一 | 全局字号 16px、页面级按钮统一 44px；二次确认统一为卡片式弹窗；暗色模式扫描结果可读性修复；添加/日志操作按钮移至标题下方 | ✅ 已完成 |
| 扫描结果展示优化 | 四列自适应（产品类型/资源名称/资源ID/地域，超长 ellipsis 截断）；空结果三态提示（未扫描引导/未找到资源/列表）；「清空」按钮红色警告 + 二次确认（厂商级） | ✅ 已完成 |
| 同步日志刷新 | 历史记录新增「刷新」按钮（重新拉取同步历史） | ✅ 已完成 |
| 文档链接更新 | `PlatformAPIDocs/` 引用同步：`AliyunSWASAPIGuide` 重命名、新增 `PlatformZoneGuide` 目录（AGENTS.md / README.md） | ✅ 已完成 |

---

## 二、设计决策记录（Build4 阶段已确认）

| # | 决策点 | 结论 | 出处 |
|---|--------|------|------|
| 1 | 主题偏好持久化 | 后端 DB（`settings` 表 `theme` 键），跨浏览器/Docker 多端一致 | Design3 §三 |
| 2 | 仪表盘布局 | 方案 B：2×2 大卡片网格 | Design3 §六 |
| 3 | 日志页「实时事件」版块 | 删除（信息由历史记录 + 运行日志覆盖） | Design3 §七 |
| 4 | Keys 缺失策略 | 仅前端提示，不阻止保存 | Design3 §9.2 |
| 5 | 历史计数口径 | 只统计实际 API 成功写入量；幂等跳过不计；重试轮重新 Diff 天然不重复计数 | Design3 §四 |
| 6 | 日志格式统一 | `slog.TextHandler` 渲染（与 stdout 逐字符一致）+ 环形缓冲回放 | Design3 §五 |
| 7 | 同步引擎状态去重 | 移除 NTag，28px 大字 + 状态色唯一展示 | Design3 §14.1 |
| 8 | "Dry Run" 更名 | 用户可见文案统一为「模拟测试」；API 端点/代码标识符保留；历史文档不动 | Design3 §14.2 |
| 9 | 主题按钮可读性 | 🌙/☀️ emoji + tooltip（零新依赖） | Design3 §14.3 |
| 10 | 运行日志折叠 | 移除折叠控件，常驻平铺（300px 内部滚动 + 1000 行上限） | Design3 §14.4 |
| 11 | 地域补全粒度 | 仅地域 ID（RegionId）自动补全，可用区信息不参与选项值；允许输入预填列表外的任意地域（不校验，云 API 自行报错） | 2026-08-03 |
| 12 | 扫描结果存储 | 按 cloud_type+region 键覆盖式入库（`scanned_resources` 表）：不同产品/地域累加，同产品同地域重扫覆盖；仅供添加目标辅助，同步流程不依赖 | 2026-08-03 |
| 13 | 资源 ID 提示形式 | 仅 placeholder 承载（移除内联说明块，保证表单三项等高对齐） | 2026-08-03 |
| 14 | 二次确认样式 | 统一 `NModal preset="card"` 卡片式弹窗（危险操作确认按钮 `type="error"` 红色） | 2026-08-03 |
| 15 | 全局视觉规范 | 字号 16px、页面级按钮 44px（`App.vue` themeOverrides）；表格内操作按钮保持小号不参与统一 | 2026-08-03 |
| 16 | 清空扫描结果范围 | 按厂商清空（该厂商全部产品），与「已扫描的资源」聚合展示范围一致 | 2026-08-03 |
| 17 | 仓库身份 | 仓库与 Go module 使用 `cloudhost-firewall-autoupdater`；产品显示名与运行时兼容标识继续使用 `FWAlizer` / `fwalizer` | 2026-09-21 |
| 18 | Desktop 范围 | 正式移除 Desktop 源码、托盘/开机自启计划与占位发布流程；历史文档仅保留当时记录 | 2026-09-21 |

---

## 三、后续设计候选（待规划，非承诺）

| # | 候选项 | 说明 | 来源 |
|---|--------|------|------|
| 1 | DNS 失败写入历史记录 | `EventDNSFailed` 落库（`result=failed`），提升故障可见性 | Design3 §13.4-A |
| 2 | 日志页「暂停输出」按钮 | 高日志量时前端暂停渲染 | Design3 §13.4-B |
| 3 | 主题设置项加入全局设置页 | 复用 `theme` 键，双入口管理 | Design3 §13.4-C |
| 4 | 模拟测试「上次执行」持久化 | 刷新页面后保留结果（当前为内存态） | 后续评估 |
| 5 | 部分成功语义说明 | 重试耗尽失败场景（如删除成功、创建失败）历史记录显示 failed 且计数为 0，云端可能有部分变更（下轮同步修正）；已知语义，不改 | Build4 核验记录 |

---

## 四、设计变更流程

1. 新设计构想先在本文件（Design4）「后续设计候选」或新章节记录（设计构想，非强制）；
2. 存在方案取舍时，使用提问工具附推荐选项与用户确认；
3. 确认后由 [Build5.md](./Build5.md) 承接为分步构建方案（Step 含目标/前置/参考代码/验收命令）；
4. 构建完成后更新本文件「当前功能全景」与「设计决策记录」。

---

## 五、变更记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0 | 2026-08-02 | 初始版本：记录当前功能全景与设计决策（承接已存档 Design1-3，见 HistoryDocs/） |
| v1.1 | 2026-08-03 | 新增功能记录：目标管理改进（资源 ID 提示/地域补全）、资源扫描与自动补全、设置页重构（凭据卡片化/清空数据/导入导出提示）、全局 UI 统一（16px/44px/卡片式弹窗） |
| v1.2 | 2026-09-21 | 仓库身份收束为 `cloudhost-firewall-autoupdater`；保留 FWAlizer 运行时兼容标识；正式移除 Desktop 开发范围 |
